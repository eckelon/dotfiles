# display arrangement generated by nwg-displays
source = ~/.config/hypr/monitors.conf
input {
  kb_layout = us
  kb_variant = intl
  touchpad {
    natural_scroll = true
  }
}
general {
  gaps_in = 4
  gaps_out = 8
  border_size = 2
  layout = dwindle
  col.active_border = rgba(cba6f7ee) rgba(89b4faee) 45deg
  col.inactive_border = rgba(313244aa)
}
layerrule = blur on, match:namespace waybar
layerrule = blur on, match:namespace nwg-dock

decoration {
  rounding = 8
  shadow {
    enabled = false
  }
  blur {
    enabled = true
    size = 5
    passes = 2
  }
}
animations { enabled = true }

$mod = SUPER
$term = ghostty
bind = $mod, Return, exec, $term
bind = $mod, W, exec, helium
bind = $mod, R, exec, fuzzel
bindr = $mod, SUPER_L, exec, fuzzel
bind = $mod, Q, killactive
bind = $mod, M, exit
bind = $mod, V, togglefloating
bind = $mod, F, fullscreen, 0
bind = $mod, J, layoutmsg, togglesplit
bind = $mod, left,  movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up,    movefocus, u
bind = $mod, down,  movefocus, d
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod_SHIFT, 1, movetoworkspace, 1
bind = $mod_SHIFT, 2, movetoworkspace, 2
bind = $mod_SHIFT, 3, movetoworkspace, 3
bind = $mod_SHIFT, 4, movetoworkspace, 4
bind = CTRL ALT, left,  workspace, e-1
bind = CTRL ALT, right, workspace, e+1
bind = CTRL ALT, 1, workspace, 1
bind = CTRL ALT, 2, workspace, 2
bind = CTRL ALT, 3, workspace, 3
bind = CTRL ALT, 4, workspace, 4
bind = CTRL ALT, 5, workspace, 5
bind = CTRL ALT, 6, workspace, 6
bind = CTRL ALT, 7, workspace, 7
bind = CTRL ALT, 8, workspace, 8
bind = CTRL ALT, 9, workspace, 9
bind = CTRL ALT, 0, workspace, 10
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
# F1: floating keybind cheat sheet (toggle on any workspace)
bind = $mod, F1, togglespecialworkspace, cheats
windowrule = float on, match:class cheatsheet
windowrule = size 620 560, match:class cheatsheet
windowrule = workspace special:cheats silent, match:class cheatsheet
bindel = , XF86MonBrightnessUp,   exec, brightnessctl -e set +5%
bindel = , XF86MonBrightnessDown, exec, brightnessctl -e set 5%-
bindel = , XF86AudioRaiseVolume,  exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume,  exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl  = , XF86AudioMute,         exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl  = , XF86AudioMicMute,      exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bind   = , Print, exec, grim -g "$(slurp)" - | wl-copy
bind   = $mod, L, exec, pidof hyprlock || hyprlock

exec-once = waybar
exec-once = nwg-dock-hyprland -d -p left -i 40 -c fuzzel -ml 6 -mt 10 -mb 10
exec-once = mako
exec-once = hypridle
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = wl-paste --type text --watch cliphist store
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = [workspace 1 silent] $term

# noop: cheatsheet window spawns on demand via $mod F1 (see binds above)
exec-once = [workspace 2 silent] helium
