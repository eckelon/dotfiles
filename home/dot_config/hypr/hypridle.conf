# screen off first (biggest battery saver), lock before suspend
listener {
    timeout = 300
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}
listener {
    timeout = 900
    on-timeout = pidof hyprlock || hyprlock
}
listener {
    timeout = 960
    on-timeout = systemctl suspend
}
